specVersion: "v1beta"
serviceId: "se-game"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "se-game-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
